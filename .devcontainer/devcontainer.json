{
	"dockerComposeFile": "docker-compose.redis.yml",
	"service": "devcontainer",
	// Change workspace subfolder according to desired project
	"workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}/maven-redis",
	// "features": {
	// 	"ghcr.io/devcontainers-contrib/features/mongosh-homebrew:1": {} // Mongosh (Shell) Feature
	// },
	"customizations": {
		"vscode": {
			"extensions": [
				"vscjava.vscode-java-pack",
				"vmware.vscode-boot-dev-pack",
				"redhat.vscode-xml"
				// "mongodb.mongodb-vscode" // MongoDB Explorer for VS Code
			]
		}
	}
}